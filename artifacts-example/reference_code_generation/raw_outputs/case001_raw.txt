=== approach ===
approach_id: approach_001
approach_name: Built-in Sort
description: Uses Python's built-in sorted() function to return a new sorted list without modifying the original.
algorithm_type: Library Function
time_complexity: O(n log n)
space_complexity: O(n)
key_characteristics: built-in, stable, simple, Timsort algorithm
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    return sorted(lst)
--- end approach ===

=== approach ===
approach_id: approach_002
approach_name: Bubble Sort
description: Implements bubble sort on a copy of the input list. Repeatedly steps through the list, swaps adjacent elements if they are in the wrong order.
algorithm_type: Bubble Sort
time_complexity: O(n^2)
space_complexity: O(n)
key_characteristics: stable, in-place (on copy), simple
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []
    arr = lst.copy()
    n = len(arr)
    for i in range(n):
        swapped = False
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped:
            break
    return arr
--- end approach ===

=== approach ===
approach_id: approach_003
approach_name: Selection Sort
description: Implements selection sort on a copy of the input list. Repeatedly finds the minimum element from the unsorted part and puts it at the beginning.
algorithm_type: Selection Sort
time_complexity: O(n^2)
space_complexity: O(n)
key_characteristics: unstable, in-place (on copy), simple
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []
    arr = lst.copy()
    n = len(arr)
    for i in range(n):
        min_idx = i
        for j in range(i + 1, n):
            if arr[j] < arr[min_idx]:
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr
--- end approach ===

=== approach ===
approach_id: approach_004
approach_name: Insertion Sort
description: Implements insertion sort on a copy of the input list. Builds the final sorted list one element at a time by inserting each element into its correct position.
algorithm_type: Insertion Sort
time_complexity: O(n^2)
space_complexity: O(n)
key_characteristics: stable, in-place (on copy), efficient for small or partially sorted data
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []
    arr = lst.copy()
    n = len(arr)
    for i in range(1, n):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr
--- end approach ===

=== approach ===
approach_id: approach_005
approach_name: Merge Sort
description: Implements top-down merge sort on a copy of the input list. Recursively divides the list into halves, sorts them, and merges the sorted halves.
algorithm_type: Merge Sort
time_complexity: O(n log n)
space_complexity: O(n)
key_characteristics: stable, divide-and-conquer, requires extra space for merging
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []

    def merge_sort(arr: list) -> list:
        if len(arr) <= 1:
            return arr
        mid = len(arr) // 2
        left = merge_sort(arr[:mid])
        right = merge_sort(arr[mid:])
        return merge(left, right)

    def merge(left: list, right: list) -> list:
        result = []
        i = j = 0
        while i < len(left) and j < len(right):
            if left[i] <= right[j]:
                result.append(left[i])
                i += 1
            else:
                result.append(right[j])
                j += 1
        result.extend(left[i:])
        result.extend(right[j:])
        return result

    return merge_sort(lst.copy())
--- end approach ===

=== approach ===
approach_id: approach_006
approach_name: Quick Sort (Lomuto partition, on copy)
description: Implements classic quicksort on a copy of the input list using Lomuto partition scheme. Returns a new sorted list without modifying the original.
algorithm_type: Quick Sort
time_complexity: O(n log n) average, O(n^2) worst
space_complexity: O(n) due to recursion stack on copy
key_characteristics: unstable, divide-and-conquer, recursive, in-place on the copy
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []

    def _quicksort(arr: list, low: int, high: int) -> None:
        if low < high:
            pi = _partition(arr, low, high)
            _quicksort(arr, low, pi - 1)
            _quicksort(arr, pi + 1, high)

    def _partition(arr: list, low: int, high: int) -> int:
        pivot = arr[high]
        i = low - 1
        for j in range(low, high):
            if arr[j] <= pivot:
                i += 1
                arr[i], arr[j] = arr[j], arr[i]
        arr[i + 1], arr[high] = arr[high], arr[i + 1]
        return i + 1

    arr = lst.copy()
    _quicksort(arr, 0, len(arr) - 1)
    return arr
--- end approach ===

=== approach ===
approach_id: approach_007
approach_name: Heap Sort
description: Implements heap sort on a copy of the input list. Builds a max-heap, then repeatedly extracts the maximum element to build the sorted order.
algorithm_type: Heap Sort
time_complexity: O(n log n)
space_complexity: O(n)
key_characteristics: unstable, in-place (on copy), comparison-based
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []

    def heapify(arr: list, n: int, i: int) -> None:
        largest = i
        left = 2 * i + 1
        right = 2 * i + 2
        if left < n and arr[left] > arr[largest]:
            largest = left
        if right < n and arr[right] > arr[largest]:
            largest = right
        if largest != i:
            arr[i], arr[largest] = arr[largest], arr[i]
            heapify(arr, n, largest)

    arr = lst.copy()
    n = len(arr)
    for i in range(n // 2 - 1, -1, -1):
        heapify(arr, n, i)
    for i in range(n - 1, 0, -1):
        arr[i], arr[0] = arr[0], arr[i]
        heapify(arr, i, 0)
    return arr
--- end approach ===

=== approach ===
approach_id: approach_008
approach_name: Counting Sort (with offset for negatives)
description: Implements counting sort on a copy of the input list. Handles negative integers by offsetting the count array. Only efficient when range of values is not too large.
algorithm_type: Counting Sort
time_complexity: O(n + k) where k is range
space_complexity: O(n + k)
key_characteristics: stable, non‑comparison, requires integer elements, linear time for small range
interface_type: function_call
entry_name: sort
parameters:
  - name: lst, type: list, required: true
return_type: list
--- code ---
def sort(lst: list) -> list:
    if not isinstance(lst, list):
        raise TypeError("Input must be a list")
    if not lst:
        return []
    if not all(isinstance(x, int) for x in lst):
        raise TypeError("All elements must be integers")

    arr = lst.copy()
    min_val = min(arr)
    max_val = max(arr)
    offset = -min_val
    range_len = max_val - min_val + 1
    count = [0] * range_len

    for num in arr:
        count[num + offset] += 1

    # Modify count to store cumulative positions
    for i in range(1, len(count)):
        count[i] += count[i - 1]

    output = [0] * len(arr)
    # Traverse from end to maintain stability
    for num in reversed(arr):
        idx = count[num + offset] - 1
        output[idx] = num
        count[num + offset] -= 1

    return output
--- end approach ===