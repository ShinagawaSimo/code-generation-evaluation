################################################################################
# Judge0 Minimal Configuration for Code-Generation Evaluation
################################################################################

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=judge0_redis_pass_2025

# PostgreSQL
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0_postgres_pass_2025

# Submission defaults
CPU_TIME_LIMIT=10
MAX_CPU_TIME_LIMIT=30
WALL_TIME_LIMIT=20
MAX_WALL_TIME_LIMIT=60
MEMORY_LIMIT=256000
MAX_MEMORY_LIMIT=512000

# Disable telemetry
JUDGE0_TELEMETRY_ENABLE=false

# Allow wait-for-result on submission
ENABLE_WAIT_RESULT=true

# Allow batch submissions
ENABLE_BATCHED_SUBMISSIONS=true
MAX_SUBMISSION_BATCH_SIZE=50

# Disable auth for local use
AUTHN_TOKEN=
