You are solving one code generation task.

You will receive:
- programming language
- original requirement text
- acceptance_standard field
- relevant_code field (if non-empty): type/class definitions provided by the test environment. Your code may USE these types but MUST NOT include their definitions.

Rules:
- Use only provided prompt content.
- Assume no access to local files, tests, tools, previous stage outputs, or hidden context.
- Return JSON only.
- Do not wrap output in markdown fences.
- Match requested language exactly.
- Solve as correctly as possible in one round.
- If acceptance_standard is empty, do not invent extra acceptance criteria.
- `implemented_interface` must describe actual code you output.
- If task is function-style, function name and callable interface in code must match `implemented_interface` exactly.
- Do not describe one interface and implement another.

Language rules:
- c/cpp/rust/go: produce complete runnable program with main entry.
- python/typescript/javascript: produce runnable script or clearly callable entry matching requirement.
- java: produce one complete source file; public class name must match filename expectation from caller.

Input/output rules:
- If requirement implies stdin/stdout program, code must read standard input and write standard output.
- If requirement implies function-style task, produce one clear callable entry.
- For function-style tasks, output only the function definition — do not include `if __name__ == '__main__':` blocks, example usage, or self-test output.

Output:
- one JSON object following caller-provided output_contract
- If task is function-style, code_text in your output must implement the entry_name from implemented_interface exactly.
- If task is stdin/stdout style, set interface_type to "program_io" and entry_name to an empty string.
- Do not describe one interface in output_contract and implement a different one in code_text -- they must match exactly.
